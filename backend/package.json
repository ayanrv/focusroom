{
  "name": "focusroom-backend",
  "version": "0.1.0",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "echo 'no build step needed'",
    "start": "npx prisma generate && npx prisma db push && tsx src/index.ts",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.17.0",
    "hono": "^4.5.0",
    "@hono/node-server": "^1.12.0",
    "zod": "^3.23.8",
    "dotenv": "^16.4.5",
    "date-fns": "^3.6.0",
    "tsx": "^4.16.2"
  },
  "devDependencies": {
    "prisma": "^5.17.0",
    "typescript": "^5.5.3",
    "@types/node": "^22.0.0"
  }
}